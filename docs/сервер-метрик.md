# Назначение

Сервер метрик предназначени для публикации состояния приложения наружу. 
Сервер слушает две ендпоинта:

POST: /state - сохраняет поля переданные в POST в памяти сервера, не удаляя те что небыло переданы ранее
GET: /state возвращает все сохранённые в памяти данные в виде JSON объекта

